// Define a class to represent the data points for the chart.
class DataPoint {
  final DateTime date;
  final double time; // Assuming time is represented as a double here

  DataPoint(this.date, this.time);
}